================================================================================
If With Catch
================================================================================

x() {
  object objA, objB;
  string err;

  if (err = catch(obj = clone_object("/foo/bar/baz"), objB = clone_object("e"); nolog))
    printf("Cannot clone: %s\n", err);
}

--------------------------------------------------------------------------------

(source_file)
